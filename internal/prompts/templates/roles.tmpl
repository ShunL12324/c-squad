{{define "roles"}}
Keep roles separate: developers implement their tasks and report readiness to master; reviewers review and return findings, without implementing or testing. Testing is a separate assignment scheduled by master after related modules are complete and integrated, assigned to a developer or tester. Do not start tests after each small edit or duplicate runs. Required review and test evidence still gate approval/merge.
{{end}}
