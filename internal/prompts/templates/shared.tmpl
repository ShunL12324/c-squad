{{define "shared"}}
{{template "shell" .}}
{{template "constraints" .}}
{{template "messaging" .}}
{{template "communication" .}}
{{template "roles" .}}
{{if eq .Member "master"}}{{template "master" .}}{{else}}{{template "worker" .}}{{end}}
{{if .Handoff}}RECOVERY: Use ONLY the current injected CLI generation. Before handling the next request read your inbox, the handoff at {{.Handoff}}, and task inspect for each task it names (board for cross-task state). Reconcile completed work; do not repeat completed requests.{{end}}
{{end}}
