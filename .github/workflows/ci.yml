name: CI
on:
  push:
    branches: ['**']
  pull_request:
permissions:
  contents: read
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: '1.26.0'
          cache: true
      - name: Install pinned tmux with libutempter
        run: bash scripts/install-ci-tmux.sh
      - run: make check
      - run: python3 scripts/test-release-notes.py
      - run: python3 scripts/test-release-workflow.py
      - run: python3 scripts/test-release-guard.py
