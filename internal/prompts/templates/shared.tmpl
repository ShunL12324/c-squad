{{define "shared"}}
{{template "shell" .}}
{{template "constraints" .}}
{{template "messaging" .}}
{{template "communication" .}}
{{if eq .Member "master"}}{{template "master" .}}{{else}}{{template "worker" .}}{{end}}
{{if .Handoff}}RECOVERY: Use ONLY the current injected CLI generation. Before handling the next request read board, inbox and handoff at {{.Handoff}}. Reconcile completed work; do not repeat completed requests.{{end}}
{{end}}
